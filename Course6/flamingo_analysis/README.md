# Flamingo Analysis

## Setup
1. Run `./setup_venv.sh` (or `python -m venv venv` and `pip install -r requirements.txt` manually if on Windows)
2. Activate the virtual environment.

## Running Tests
Run `pytest tests/`

## Running Scripts
Run `python src/aggregate_exercises.py` or other scripts.
