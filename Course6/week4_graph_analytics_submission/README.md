# Week 4 Neo4j Graph Analytics
This directory contains the final deliverables for the Week 4 Graph Analytics assignment.

Run the analysis by spinning up `docker compose up -d` and executing the scripts in the `cypher/` directory via `cypher-shell`. All insights and calculations are fully documented in `results.md`.
